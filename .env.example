# Copy to .env — never commit the real one.
PORT=3000
SQLITE_PATH=./push.sqlite

# Generate with: npx -y web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@example.com
